version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 10
      docker: 18
      java: openjdk11
  pre_build:
    commands:
      - java -version
      - mvn --version
      - node --version
      - npm --version
      - docker --version
  build:
    commands:
      - cd backend
      - mvn clean package -DskipTests=true -q
      - cd ..
      - cd frontend
      - npm ci --quiet
      - npm run build:prod
      - cd ..
      - docker build . -t kafka-viewer
